# Rust Guide

Rust applications should use the native crates directly.

## Quick Start

```sh
cargo run --manifest-path examples/auth-demo/rust/Cargo.toml
```

Relevant files:

- `examples/auth-demo/rust/Cargo.toml`
- `examples/auth-demo/rust/src/main.rs`
- `tafrah/src/lib.rs`
