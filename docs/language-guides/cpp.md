# C++ Guide

Use the installed C++ wrapper header over the C ABI.

## Quick Start

```sh
make install PREFIX="$PWD/dist/install"
sh examples/auth-demo/run_language_examples.sh
```

Relevant files:

- `tafrah-abi/include/tafrah/tafrah.hpp`
- `examples/auth-demo/cpp/main.cpp`
