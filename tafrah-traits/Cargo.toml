[package]
name = "tafrah-traits"
version = "0.1.0"
edition = "2021"
description = "Shared traits and error types for the Tafrah PQC library"

[features]
default = ["std"]
std = ["alloc"]
alloc = []

[dependencies]
rand_core = { workspace = true }
zeroize = { workspace = true }
